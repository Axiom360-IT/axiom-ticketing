import { ComingSoon } from "@/components/shared/coming-soon";

export default function TicketsPage() {
  return (
    <ComingSoon
      title="Tickets"
      description="The full ticket queue, search, and detail view."
      module="M3 — Ticket lifecycle"
    />
  );
}
